import React from 'react';
import { Box, Typography } from '@mui/material';

const FaceEmbeddings: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>Face Embeddings</Typography>
      <Typography>Face embeddings management will be implemented here.</Typography>
    </Box>
  );
};
export default FaceEmbeddings;