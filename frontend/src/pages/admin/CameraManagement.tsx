import React from 'react';
import { Box, Typography } from '@mui/material';

const CameraManagement: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>Camera Management</Typography>
      <Typography>Camera discovery and management features will be implemented here.</Typography>
    </Box>
  );
};
export default CameraManagement;